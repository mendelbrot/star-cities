---
date: 2026-03-13
---

## Developer Logs

Today is the end of week 3. This is the deadline to have a playable game. The only essential item that's missing is the rules. 

**rules page**

I'd like to create a /rules page in markdown with tables and diagrams for ship icons. 
The rules page can go with this to start:

* notes-star-cities/docs/game_rules.md

And the ship icons are here:

* notes-star-cities/assets/ship-icon-svgs

Let's navigate to the rules page from the lobby page. Beside left of the profile icon in the app bar, we'll put an icon that's like an olden days scroll to link to it. And we'll put a back arrow in the rules page to nav back to the lobby, just like in the profile page.

**data streaming bug**

There's a bug to look into. When I'm on the game screen and i leave the app open for a while and then get back to it, I see a websocket error captured here:

* notes-star-cities/screen-captures/signal-2026-03-13-084850.png

(It says Error loading vision: RealtimeSubscribeException, and the status is timedOut)

After this the lobby screen doesn't show an error but it no longer updates: new games don't show up on the lists. 

So it looks like when the websocket connections time out, the app breaks and needs to be restarted. What it should do instead is re-do the REST -> Stream -> REST cycle to load the data while re-establishing the connection in the background. 

Here's is the component we'll need to work on:

* client-flutter/lib/shared/providers/robust_stream_provider.dart

**unanticipated gameplay UI behavior**

I tested multiplayer gameplay and it works. There is a UI behavior that logically follows from my specifications, but in gameplay it is very misleading and looks like a bug. (I don't notice it playing as a single player because the turn resolves very quickly)

When I press 'done' the planned actions are sent to the server. They're also deleted from the gameplay UI and the game planning board and planning panel reset to their original state at the beginning of the turn. 

Let's try this. The players table has a is_ready column. When the user submits their planned actions to the turn_planned_actions table, they set this at the same time. (it would be better if this were set by a db trigger, and if it were named is_turn_actions_submitted, but we won't change it for now. I'll make a note to do the name change and trigger later.) If is_ready is true: then let's disable the game planning board and planning panel, like we already do when status isn't PLANNING. And we'll also display text in the planning panel: where it says "No selection", We'll put "Turn actions submitted". In fact, let's make this simpler. Let's consolidate and just do one conditional: 'game status !== PLANNING || is_ready = true'. If this is true, disable UI and display the text specified above in that space.

**task list**

**priority 1**
1. add a rules page
2. fix the data streaming bug
3. fix the gameplay UI behavior on submitting turn
3. Set margin around game planning board back to 16 (Only in the planning tab!)
4. In the events tab, when the turn changes clear all events selections and ensure that the new current turn is the default selected turn
5. In the events tab, for maneuvers and advances. Just show the replay board (full size) (and delete the list of move events) (make sure to still keep the board size constraint for bombardments and battles!)

**priority 2 (not for today)**
1. rename players is_ready to is_turn_actions_submitted, and have it set by a trigger on turn_actions
2. Consolidate and correct the snapshot stages in preparation for 2.3.
3. Visibility needs to be moved to the backend It's not feasible to calculate visibility in three places (resolve-turn, the frontend, and player-bot too). Also, the turn_events table is getting messy: It has snapshots and player_ranking. These will be moved out of this table. We're going to create a new table player_turn_data, that for player-specific snapshots and events, and ranking too, for each turn. it will have a unique constraint (game_id, player_id, turn_number).
4. Further clean up event cards content, and revise the list of events

## Agent Logs


- Added a rules page accessible from the lobby via a scroll icon button. Used flutter_markdown to render game_rules.md and added ship icon SVG assets for visual rules.
- Fixed the data streaming bug by adding status monitoring and automatic retry logic to RobustSupabaseNotifier. If a websocket connection times out or fails, it now re-syncs via REST and attempts to re-establish the subscription.
