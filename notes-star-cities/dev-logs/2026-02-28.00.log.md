---
date: 2026-02-28
---

## Developer Logs

Reviewing logic in resolve-turn function:
* 36 I realized we also need to add a wasJustAnchored field, and make it so if there's a de-anchor event when wasJustAnchored=true, then set wasJustAnchored=false and wasJustDeanchored=false. (The intension is during planning to allow the player to anchor, then change their mind and de-anchor and still be able to move the star city instead)
* 199 all parameter numbers, like max tethered ships per city, max distance from city, bombard strength, etc must be declared as named constants: no unexplained parameter numbers in the code.
* 474 possible strange naming. it looks like defenderId is a coordinate, and on the next line defender is actually the defender id? Do I understand this correctly? if so, we must fix the names.
* 496 When creating a battle, moves into the battle square where the mover is the same side as the defender should filtered out, and do not get added to the entering participants list. (Must check if this is already handled or if it needs to be).
* 532 make the function for a weighted roll a standalone shared function (can also use it for calculating bombardment etc and would make code easier to read.) Are there other parts that can be refactored to functions for better readability?
* 611 the loop for applying moves until no more moves can be made is about the same for moves before battle and moves after battle, would it make sense to factor this into a function for re-use?
* several type errors in the latter part of the code.

## Agent Logs

* Defined game logic constants (strengths, ranges, etc.) to replace magic numbers.
* Implemented `wasJustAnchored` logic in `ANCHOR_ACT` to support undoing anchor during planning.
* Refactored battle and bombardment resolution to use a new shared `weightedRoll` function.
* Extracted move application logic into a shared `applyNonConflictingMoves` function.
* Renamed `defenderId` to `occupantId` and clarified battle participant filtering.
* Renamed `lossCascade` to `handleTetherLoss` and removed recursion (Star Cities don't tether to Star Cities).
* Updated win condition and acquisition logic to use game parameters and constants.
* Corrected typo in `game_rules.md` regarding Star City tethering and updated function name across all documentation.
* Fixed Deno type errors in `resolve-turn/index.ts`.
