---
date: 2026-02-28
---

## Developer Logs

Reviewing logic in resolve-turn function:
* 36 I realized we also need to add a wasJustAnchored field, and make it so if there's a de-anchor event when wasJustAnchored=true, then set wasJustAnchored=false and wasJustDeanchored=false. (The intension is during planning to allow the player to anchor, then change their mind and de-anchor and still be able to move the star city instead)
* 199 all parameter numbers, like max tethered ships per city, max distance from city, bombard strength, etc must be declared as named constants: no unexplained parameter numbers in the code.
* 474 possible strange naming. it looks like defenderId is a coordinate, and on the next line defender is actually the defender id? Do I understand this correctly? if so, we must fix the names.
* 496 When creating a battle, moves into the battle square where the mover is the same side as the defender should filtered out, and do not get added to the entering participants list. (Must check if this is already handled or if it needs to be).
* 532 make the function for a weighted roll a standalone shared function (can also use it for calculating bombardment etc and would make code easier to read.) Are there other parts that can be refactored to functions for better readability?
* 611 the loop for applying moves until no more moves can be made is about the same for moves before battle and moves after battle, would it make sense to factor this into a function for re-use?
* several type errors in the latter part of the code.

All of the above items have been addressed!

Review round 2:
* 42: the interface ValidatedMove isn't used. please look into where it would/should be used and what's used instead.
* I just discovered an interesting situation that would have been obvious from playing: I incorrectly specified how to use the is_stunned field. With the current logic, bombardment prevents movement on the current move and also prevents all action on the next move. I've decided that I only want it to prevent movement on the current move. To handle this change, i'd like to add a wasJustBombarded field to the PieceTurnContext type, then use this instead of is_stunned in the logic. Now, the is_stunned field should never be read or set. Let's remove is_stunned from the data model and from the database schema, and update the documentation and the game rules to clarify this.
* In Phase 5: Win/Elimination, the game status shouldn't be set to finished because it is set in phase 7 (Phase 7 is the correct place to update this, because the game status shouldn't be updated until after all processing is finished and event and state records created)
* ensure table names are correct throughout:
  * games
  * players
  * turn_planned_actions
  * turn_events
  * turn_states

All the above items have also been addressed!

Now I want to do some changes/consolidations to the phases, and refactor of resolve-turn function.
Gemini created a file with recommendations for me here:
/notes-star-cities/knowledge/turn_resolution_refactoring.md
I reviewed and I agree with the recommendations. I'd like Gemini to give it another read, looking for any potential oversights, and ask me any clarifying questions before proceeding with the refactoring.

## Agent Logs

* Defined game logic constants (strengths, ranges, etc.) to replace magic numbers.
* Implemented `wasJustAnchored` logic in `ANCHOR_ACT` to support undoing anchor during planning.
* Refactored battle and bombardment resolution to use a new shared `weightedRoll` function.
* Extracted move application logic into a shared `applyNonConflictingMoves` function.
* Renamed `defenderId` to `occupantId` and clarified battle participant filtering.
* Renamed `lossCascade` to `handleTetherLoss` and removed recursion (Star Cities don't tether to Star Cities).
* Updated win condition and acquisition logic to use game parameters and constants.
* Corrected typo in `game_rules.md` regarding Star City tethering and updated function name across all documentation.
* Fixed Deno type errors in `resolve-turn/index.ts`.
* Removed `is_stunned` field from `Piece` model and replaced with `wasJustBombarded` in `PieceTurnContext`.
* Refactored bombardment to only prevent movement in the current turn.
* Consolidated game status and winner updates into Phase 7 of turn resolution.
* Removed redundant `ValidatedMove` interface definition.
* Updated `start-game` function and all documentation to reflect the removal of `is_stunned`.
* Verified table names across documentation and codebase.
* Refactored `resolve-turn` function into a modular phase-based architecture (`01-prepare`, `02-intent`, `03-combat`, `04-lifecycle`, `05-finalize`).
* Implemented `TurnContext` class to manage piece state, indexes, and event accumulation.
* Extracted game logic constants into `constants.ts` and shared utilities into `utils.ts`.
* Updated all phases to use the `TurnContext` and common transaction object.
* Fixed several Deno type errors across all phases by adding explicit types to arrow function parameters.
* Ensured `Piece` and other types are correctly imported in all modularized files.
* Verified type correctness with `deno check`.
* Renamed shadowed `sql` variable to `tx` in `resolve-turn/index.ts` to resolve VS Code Deno extension errors.
* Removed unused variables and imports in `phases/02-intent.ts`, `phases/03-combat.ts`, and `context.ts`.
* Introduced `Player` type in `_shared/types.ts` and applied it across all resolution phases for better type safety.
* Replaced `any` with `postgres.Sql` in phase function signatures to improve database transaction type safety.
* Resolved all remaining lint warnings (`no-unused-vars`, `no-explicit-any`).
* Verified final code with `deno lint` and `deno check`.
* Implemented `handle_player_ready` Postgres trigger in `supabase/schemas/05_triggers.sql` to automate turn resolution when all non-eliminated players are ready.
