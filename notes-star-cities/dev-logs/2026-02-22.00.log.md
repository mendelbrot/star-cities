---
date: 2026-02-22
---

## Developer Logs

Today I'm starting a new project, Star Cities. It's a simultaneous move multiplayer game.
Starting the project by copying my Dax project and clearing out everything relating to Dax, renaming, and deleting the Flutter client and making a new one, and copying the relevant files from the old Flutter clients into the new clients.
I still need to set up projects in Supabase, Netilify, and GitHub (and later, fly.io for the game server).
The first thing I'll need help with is cleaning up some issues in Flutter and writing the lobby page to verify that the app will start.
Then we'll start work on the database schema.
Then later we'll build a game server in Go.

I created a supabase project.
I had to update an email notification setting in the supabase dashboard:
```
<h2>Star Cities</h2>
<h3 style="font-size: 32px; letter-spacing: 8px;">{{ .Token }}</h3>
```


## Agent Logs
- Created `client-flutter/lib/features/lobby/presentation/screens/lobby.dart` with a placeholder `LobbyPage` widget.
- Updated `client-flutter/lib/core/router/app_router.dart` to use `LobbyPage` as the initial route.
- Refactored `client-flutter/lib/app.dart` to remove Riverpod dependencies and use `StatelessWidget`.
- Ran `flutter pub get` to ensure all Flutter dependencies are installed.
- Added `const` to the `App()` constructor in `client-flutter/lib/main.dart`.
