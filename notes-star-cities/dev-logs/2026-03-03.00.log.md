---
date: 2026-03-03
---

## Developer Logs

polishing milestone 2 continued

1. I tested Star Cities on the browser and found that the url paths aren't updated when I navigate to a page. Why? 
2. I thought more about the user setup logic: there only needs to be a /profile page (not /setup page required as I originally though). This is how it currently is. The profile page should have a message asking the person to choose a username, that is conditionally rendered if they don't have one yet.
3. The profile page should have a link to sign out.
4. Signing out should invalidate all providers, so no data will carry over when signing into a different account.
5. in the username field, remove the placeholder about the "callsign"
6. the username form should perform appropriate frontend validation
7. the username for should check username availability
8. it should do both of these in real time as the user types (with a short debounce for the database check, and only check if it passes validation)
9. remove the user icon selection all-together from the frontend profile page, they will only choose a username for now
10. the lobby page should have a link to the profile page (to the left of the sign-out link)
11. in the game page, remove the delete game link (it is not needed because the game is auto-deleted when abandoned)
12. the create game button styling is not consistent with the theme
13. in all styling, make sure colors are used from the theme, no hard-coded colors (except the faction colors that will be handled later)
14. the UX for creating a new game doesn't flow that smoothly: it is seen created in the lobby list before navigating to the game page. Please offer a proposal to make this process more appear more streamlined
15. in the lobby, there should be four lists:
  - TAP Required
  - TAP Done, Waiting for Others
  - Waiting for Players to Join
  - Open Games
(TAP stands for Turn Action Plan)
Please offer a proposal to add appropriate database indexes or views for efficient querying if practical
16. I Was not abler to test the game screen (for adding players) very well, because it seems that changes don't update in the UI correctly. For example, when I try to delete a bot player, it isn't deleted in the UI. Please check how the provider is implemented and used to ensure that all changes to the database are immediately reflected in the UI
17. The UI delete player buttons should only be displayed for bot players, not user player.
18. There should be a leave game button for the current user to delete their player from the game
19. When a user navigates toa n open game, they should NOT be added to the game
20. there should be a join game button (in the same place as the leave game button) that the user sees if they haven't joined the game yet

## Agent Logs

*   Drafted the "Milestone 2 Polishing Strategy" knowledge document.
*   Initiated Phase 1: Database & Logic overhaul.
*   Enabling `usePathUrlStrategy()` in `main.dart` for correct web URL bar updates.
*   Simplified `AppStateManager` to rely exclusively on Auth Metadata for router redirects.
*   Proposed a new Postgres View `v_user_game_status` to support the 4-list Lobby layout efficiently.
*   Refactored `ProfileSetupPage` with realtime username availability check and Sign Out link.
*   Implemented the 4-list Lobby layout using Riverpod and the new DB view.
*   Enabled `REPLICA IDENTITY FULL` on all database tables to fix realtime deletion bugs.
*   Refactored `GameBoard` with Join/Leave logic and restricted bot deletion.
*   Implemented `authStateProvider` to invalidate all Riverpod state on sign-out.
*   Completed final styling audit and fixed deprecation warnings.
*   Verified clean build with `flutter analyze`.
*   Fixed Lobby realtime sync issue by refactoring `lobby_providers.dart` to stream from base tables instead of the view.
*   Updated `v_user_game_status` view with missing `created_at` and `player_count` fields for future use.

