---
date: 2026-03-07
---

## Developer Logs

Working through items form yesterdays review and brainstorming.

## Agent Logs

* Read and analyzed recent dev logs and game documentation to synchronize on project state.
* Created a summary and analysis file in \`notes-star-cities/knowledge/dev_log_analysis_2026_03_06.md\`.
* Flattened the frontend feature folder structure (\`client-flutter/lib/features/\`) into standardized \`models/\`, \`providers/\`, \`screens/\`, and \`widgets/\` directories.
* Updated all import paths and verified the project with \`flutter analyze\`.
* Refactored \`ShipIcon\` into a \`StatelessWidget\` using inline SVG string templates.
* Created \`lib/shared/utils/ship_svg_templates.dart\` to manage colored SVG generation.
* Removed unused SVG files from \`assets/ships/\` and cleaned up \`pubspec.yaml\`.
* Refactored \`ShipBanner\` to be a row of interactive \`RandomShip\` widgets that change on tap.
* Moved the \`ShipBanner\` to the top of the \`SignInPage\` and made the ship count configurable.
* Added "STAR CITIES" title to the \`SignInPage\` below the banner.
* Implemented \`replay_step\` for turn events to support sequenced playback in the UI.
* Updated \`TurnEvent\` types in \`supabase/functions/_shared/types.ts\` to include an optional \`replay_step\` field.
* Modified \`TurnContext\` in \`resolve-turn\` function to manage \`currentStep\` and automatically inject it into all generated events.
* Sequenced \`resolve-turn\` phases into 7 distinct replay steps: Structural (1), Bombardment (2), Primary Movement (3), Combat (4), Resolution Movement (5), Lifecycle (6), and Conclusion (7).
* Updated \`notes-star-cities/docs/data_models_and_game_logic.md\` with the new \`replay_step\` field and mapping.
* Verified \`resolve-turn\` function with \`deno check\`.
* Refactored \`GameRoom\` into a multi-screen architecture.
* Created \`GameScreen\` as the main entry point to toggle between \`GameRoom\` (waiting) and \`GamePlay\` (active).
* Extracted reusable widgets into \`lib/features/game/widgets/\`: \`SectionTitle\`, \`PlayerListItem\`, and \`GameRoomControls\`.
* Implemented \`GameController\` to centralize database interactions for game actions.
* Updated \`GoRouter\` configuration to use the new \`GameScreen\`.
* Verified the refactoring with \`flutter analyze\`.
* Initialized gameplay UI foundation:
    * Updated \`Game\` model with \`stars\` and \`Player\` model with \`homeStar\`.
    * Implemented \`gameplayUiProvider\` for managing selection, hover, and replay state.
    * Created \`turnStateProvider\` (now deprecated) and replaced it with REST-based \`gameplayTurnStateProvider\`.
    * Implemented \`GameBoard\` widget with torus-wrapped 9x9 grid, centered on player's home star.
    * Integrated \`GameBoard\` into \`GamePlay\` screen for Replay and Planning tabs.
    * Cleaned up deprecated Flutter API usage and verified with \`flutter analyze\`.
* Refactored Turn Data fetching:
    * Created \`TurnState\` and \`TurnEventList\` models with built-in mapping logic.
    * Implemented \`gameplay_providers.dart\` using Supabase REST queries instead of streams.
    * Added conditional re-fetching logic to providers to only trigger when the game enters the \`PLANNING\` status.
    * Parameterized providers with \`gameId\` using Riverpod family modifiers for robust multi-game support.
    * Updated \`GameBoard\` to consume the new structured models and providers.
* Overhauled Game Action and Event models:
    * Created \`game_actions.dart\` and \`game_events.dart\` with strongly-typed models for every action and event type.
    * Utilized Dart \`sealed\` classes to provide TypeScript-like union types for \`GameAction\` and \`GameEvent\`.
    * Implemented comprehensive \`fromMap\` and \`toMap\` methods for all subtypes.
    * Cleaned up \`game_models.dart\` to only contain core game entities (\`Piece\`, \`TurnState\`, etc.).
    * Verified type safety and exhaustive pattern matching support with \`flutter analyze\`.
* Renamed \`GameScreen\` to \`Game\` and moved it to \`game.dart\` to follow project naming conventions.
* Used aliased imports for the \`Game\` data model to avoid naming collisions with the \`Game\` widget.
* Implemented "Fog of War" vision system:
    * Created \`GameConstants\` to centralize vision ranges and game rules.
    * Implemented \`vision_logic.dart\` for calculating torus-wrapped visible squares.
    * Created \`visionProvider\` to manage reactive visibility state for current and previous turns.
    * Refactored \`GameBoard\` to be a data-driven widget that handles visibility-based rendering (shading non-visible tiles, hiding enemy units/stars).
    * Updated \`GamePlay\` to pass sequenced turn data and vision sets to the board.
* Implemented \`Faction.random()\` to ensure random faction assignment for all players, including the game creator.
* Updated \`addBot\` and \`joinGame\` to ensure unique bot names and faction assignments.
* Refactored game settings chips into a shared \`ResponsiveGameHeader\` widget.
* Fixed layout issues where game settings chips would overflow on small screens in \`GameRoom\` and \`GamePlay\`.
* Unified the responsive stacking behavior of game info across the Lobby, Game Room, and Game Play screens.
* Simplified \`ResponsiveGameHeader\` with a single responsive breakpoint (600px) and removed the chips column layout.
* Replaced the bulky \`GameSettingsRow\` widget with a lightweight, reusable \`GameSettingsChip\` component.
* Verified the frontend changes with \`flutter analyze\`.
* Implemented the \`PlanningPanel\` widget for the gameplay screen:
    * Created \`lib/features/game/widgets/planning_panel.dart\` as a new component.
    * Integrated the \`PlanningPanel\` into the \`GamePlay\` screen's Planning tab.
    * Implemented a pieces tray showing the current player's units available for placement.
    * Added contextual action buttons (Anchor/De-anchor, Re-tether, Bombard) based on the selected ship type.
    * Added "Reset" and "Done" buttons to the planning interface.
    * Updated the \`GameBoard\` widget to support interactive piece selection with visual highlighting.
    * Implemented the local action buffering system:
        * Created \`pendingActionsProvider\` to manage turn actions before submission.
        * Updated \`GameController\` with \`submitActions\` and \`resetActions\`.
    * Overhauled \`gameplayUiProvider\` to manage multi-step placement states.
    * Implemented the 3-step (Piece -> City -> Square) and 2-step (Piece -> Square) placement flows.
    * Enhanced the \`GameBoard\` with:
        * Faction-colored tether lines using \`CustomPainter\`.
        * Visual indicators (filled circles) for available squares during placement/movement.
        * "Virtual State" rendering that applies pending actions to the board with reduced opacity.
    * Updated \`PlanningPanel\` to provide contextual instructions for placement steps and grey out used tray pieces.
    * Verified the implementation with \`flutter analyze\`.
    * Integrated \`GameParameters\` class into the \`Game\` model for structured access.
    * Enforced Star City de-anchor rules in \`PlanningPanel\` (only allowed if no ships are tethered).
    * Simplified \`PlanningPanel\` instructions to focus on current selection/placement status.
    * Implemented visual emphasis for anchored Star Cities with available capacity during 3-step placement.
    * Updated \`ResponsiveGameHeader\` to use the new structured \`GameParameters\`.
    * Refined the gameplay UI and rules enforcement:
        * Set the "Planning" tab (index 2) as the default tab in \`GameScreen\` screen by moving and configuring the \`DefaultTabController\` to correctly manage the \`TabBarView\` and \`TabBar\`.
        * Fixed an issue where nested \`DefaultTabControllers\` prevented tab switching.
        * Removed the redundant "Cancel Placement" button from \`PlanningPanel\`.
        * Enhanced anchoring rules to account for pending actions: Star Cities can no longer de-anchor if any ships are tethered in the "virtual" turn state (including newly placed or re-tethered ships).
        * Updated \`GameBoard\` and \`PlanningPanel\` to apply \`TetherAction\` to the virtual piece state, ensuring accurate capacity checks.
    * Implemented the movement system for the planning phase:
        * Created \`_calculateAvailableMoves\` with range, star, and tether constraints.
        * Added logic to prevent multiple friendly units from targeting the same square in a turn.
        * Implemented \`MoveArrowPainter\` to draw white arrows for planned moves (skips torus-wrapped moves for now).
        * Refined movement planning: pieces now stay in their original position visually, with a dimming effect and arrow indicating the target.
        * Allowed moving to squares occupied by friendly units to support multi-ship move planning.
        * Updated \`_handleSquareTap\` to replace existing moves for a piece when a new move is planned.
        * Refined \`GameController.resetActions\` to clear all UI selection and planning states (piece selection, placement mode, bombardment mode) in addition to clearing pending actions.
        * Refined \`GameController.submitActions\` to clear all local pending actions and UI selection states upon successful submission.
        * Fixed an issue where units couldn't plan moves into squares occupied by opponent ships; tapping a piece on an available square now correctly triggers a move/placement target action.
        * Disabled selection and actions for pieces that were just placed in the same turn.
        * Added "illegal square tap" to cancel piece/placement selection.
        * Added \`isPlanning\` flag to \`GameBoard\` to isolate replay boards from planning-specific logic like move arrows and local UI selection state.
    * Refined planning phase logic:
        * Implemented \`addOrReplaceAction\` in \`PendingActionsNotifier\` to allow ships (like Eclipse) to both move and bombard in the same turn without clearing previous actions.
        * Updated piece rendering: pieces with planned moves no longer dim, maintaining full visibility for subsequent actions.
        * Enhanced bombardment targeting: selectable enemy targets now feature a white dashed border (implemented via \`DashedCirclePainter\`), with the solid border removed for better visual distinction.
        * Enforced Star City action restrictions: cities cannot plan both a movement and an anchor/de-anchor action in the same turn.
    * Implemented the bombardment system:
        * Added \`isBombarding\` to \`GameplayUiState\` to manage targeting mode.
        * Updated \`PlanningPanel\` with a "Bombard" button for Eclipse units that toggles targeting mode.
        * Created \`_calculateAvailableBombardSquares\` to identify valid enemy targets within range (2).
        * Enhanced \`GameBoard\` to show white selection borders for bombardment targets.
        * Updated \`BombardPainter\` to visualize planned bombardments with white dashed lines and crosshair icons on targets (changed from red to avoid faction color confusion).
        * Updated \`_handlePieceTap\` to create \`BombardAction\` and exit targeting mode when a target is selected.
    * Finalized Planning Phase UI cleanup:
        * Renamed the "Players" tab to "Info" in the \`GameScreen\` AppBar.
        * Removed the "Pieces Tray" title and instructional help text from the \`PlanningPanel\` for a cleaner interface.
    * Optimized Planning Phase UI for better space efficiency and responsiveness:
        * Implemented a responsive reflow in \`GamePlay\`: the layout now automatically switches between a horizontal row (wide screens) and a vertical column (mobile/small screens).
        * Refined the horizontal layout using \`AspectRatio\` and optimized spacing to ensure the \`PlanningPanel\` stays tucked beside the board grid (16px total gap).
        * Standardized \`PlanningPanel\` with a consistent \`maxWidth\` of 350px across all layouts.
        * Styled ship actions (Anchor, Re-tether, Bombard) as high-contrast, square-rounded icon buttons (white background, black icon) with tooltips.
        * Updated \`GameBoard\` layer order: movement arrows, tether lines, and bombardment indicators are now rendered on top of piece icons for better visibility during planning.
        * Repositioned ship actions below the selection status text, aligned to the left.
    * Verified the UI logic with \`flutter analyze\`.
* Updated `PlanningPanel` to maintain a constant vertical height by ensuring the selection status text and action icons (using a transparent placeholder if needed) are always rendered.
* Increased `PlanningPanel` maxWidth to 370 to ensure 5 tray pieces fit without scrolling.
* Modified `PlanningPanel` constraints to remove `maxWidth` and apply a `maxHeight` of 1200.
* Restored `maxWidth: 370` and removed `maxHeight` in `PlanningPanel`.
* Wrapped the entire Planning tab view in a `ConstrainedBox` with `maxHeight: 1200` and no `maxWidth`.
* Removed `maxWidth` constraint from `PlanningPanel` and switched to `MainAxisSize.max` for the Planning tab's horizontal layout to maximize available space.
* Restored `maxWidth: 370` to `PlanningPanel`.
* Removed the global `maxWidth: 1000` constraint from the `GameScreen` during active gameplay to allow the Planning tab to use full horizontal space.
* Applied the `maxWidth: 1000` constraint specifically to the `GameRoom`, `Info` tab, and `Replay` tab to maintain layout consistency while freeing the Planning tab.
* Modified `GamePlay` to ensure the `PlanningPanel` maintains a fixed width of 370 in wide-screen Row mode, allowing the `GameBoard` to shrink if necessary.
