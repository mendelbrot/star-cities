---
date: 2026-03-01
---

## Developer Logs

Tested triggering the resolve-turn function by creating/updating db records in the supabase dashboard. Fixed a bug where it was saving turn_states record to the current turn instead of the next turn. Enforced unique constraint on the turn_planned_actions table of one action list per player per turn. Currently allowing upsert for easier redoing/debugging in development.

## Agent Logs

* Enforced unique constraint on `turn_planned_actions` for `(game_id, player_id, turn_number)` in `supabase/schemas/02_tables.sql`.
* Updated `player-bot` edge function to use idempotent `UPSERT` logic for planned actions.
* Updated `resolve-turn` edge function (Phase 5) to correctly insert `turn_states` for the *next* turn (`turn_number + 1`) and used idempotent `UPSERT` logic for both `turn_events` and `turn_states`.
* Updated `start-game` edge function to use idempotent `UPSERT` logic for the initial `turn_states` insert.
* Verified and documented unique constraints for `turn_states` and `turn_events` in `notes-star-cities/docs/data_models_and_game_logic.md`.
