---
date: 2026-03-12
---

## Developer Logs

## Agent Logs

* Investigated and fixed a `TypeError: Cannot read properties of undefined (reading 'faction')` in `resolve-turn` Edge Function (Phase 3: Combat).
* Refactored `TurnContext.removePiece()` to ensure that pieces are also removed from `pendingPlacements`, preventing crashes when a piece is placed and then destroyed (via bombardment or tether loss) in the same turn.
* Cleaned up `TurnContext.handleTetherLoss()` to use the centralized `removePiece()` method, ensuring consistent board state index updates.
* Added defensive checks in `resolveCombat` to safely filter out missing pieces during battle detection and resolution loops.
* Verified the fixes by reviewing the logic flow in `03-combat.ts` and `context.ts`.
* Refactored Combat Resolution (Phase 3) for true simultaneity:
    * Deployed a batching strategy for battle resolution: all battles are now calculated state-free before any pieces are modified.
    * Deferred all piece destruction and tether losses until after all battle outcomes are determined, ensuring that pieces lost in one battle can still participate in concurrent battles.
    * Centralized piece removal and tether loss processing to eliminate logic duplication and potential race conditions in board state indexes.
    * Removed redundant board state snapshots between Phase 2 and Phase 3 to optimize server performance.
    * Improved code modularity in `03-combat.ts` by separating battle identification, resolution, and result application into distinct, sequential steps.
* Verified the refactored backend logic with `deno check` and `deno lint`.
